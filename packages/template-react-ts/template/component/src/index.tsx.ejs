import * as React from 'react';
import './index.scss';

type ComponentProps = {
  title: string,
};

export default function <%= className %>(props: ComponentProps) {
  const { type, ...others } = props;

  return (
    <div className="<%= name %>" {...others}>Hello <%= className %></div>
  );
}
