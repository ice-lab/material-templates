{
  "type": "rax",
  "targets": ["<%- projectTargets.join('", "') %>"],
  <% if(projectTargets.indexOf('miniapp') > -1 && buildType === 'runtime') {%>
  "miniapp": {
    "buildType": "runtime"
  },
  <%}%>
  "plugins": [
    "build-plugin-component"
    <% if(isAliInternal && projectTargets.indexOf('miniapp') > -1 && buildType === 'runtime') {%>
      ,"@ali/build-plugin-miniapp-preview"
    <%}%>
  ]
}
